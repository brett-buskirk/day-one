# Dependencies
node_modules
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
*.local

# Generated content bundle (regenerated on every dev/build by the pipeline)
src/content/corpus.generated.json

# Service worker / Workbox dev artifacts
dev-dist/

# Test / coverage output
coverage/
*.lcov

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Caches
.vite/
.cache/
node_modules/.tmp/
.eslintcache

# Environment / secrets
.env
.env.*
!.env.example

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Private working notes — playtest reviews, feedback, and run dumps stay local, not public.
# (The reusable Plaud → Claude Code template under docs/plaud/ IS committed; the raw reviews
# that template produces are not.)
docs/reviews/
